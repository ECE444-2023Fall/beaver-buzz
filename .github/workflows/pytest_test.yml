name: Run Python unit tests

on: [push]

env:
  SQLALCHEMY_DATABASE_URI: postgres://qecwoigoozrgyp:e50b6d4b10e8402b7f0fa17b0c5fd89e54ff5913df327e3e20e365f27f0208fc@ec2-54-156-20-209.compute-1.amazonaws.com:5432/decpblpm06ivfj


jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10"]

    steps:
      - uses: actions/checkout@v3
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r ./api/requirements.txt
      - name: Running unit tests with pytest
        run: |
          python -m pytest ./api/utils/unit_tests/
